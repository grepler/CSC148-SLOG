# so this file makes me feel dirty, but I guess I understand why it's here?
# allows you to run initialization code prior to import of a module from the package,
# for example, checking dependencies, but why isn't that part of the module?
# perhaps because if you import a single class from a module,
# the validation code wouldn't run?
